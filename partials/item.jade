.item
  p \Название: 
    #{item.name}
  p \Добавлено: 
    - var n = new Date(item.added)
    #{item.added}
  p \Вес: 
    #{item.weight}
  #complectations!= partial('complectation', item.complectations)
\|
